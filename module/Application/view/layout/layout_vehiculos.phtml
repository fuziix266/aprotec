<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content="Sistema de Identificación Vehicular Municipal mediante Código QR - APROTEC" />
    <title><?= $this->headTitle('Vehículos Municipales QR - APROTEC')->setSeparator(' — ')->setAutoEscape(false) ?></title>

    <!-- Google Fonts: Inter -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <!-- Bootstrap 5 -->
    <link href="<?= $this->basePath('assets/vendors/bootstrap/css/bootstrap.min.css') ?>" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="<?= $this->basePath('assets/vendors/bootstrap-icons/bootstrap-icons.min.css') ?>" rel="stylesheet">
    <!-- SweetAlert2 CSS -->
    <link rel="stylesheet" href="<?= $this->basePath('assets/vendors/sweetalert2/sweetalert2.min.css') ?>">
    <!-- DataTables CSS -->
    <link rel="stylesheet" href="<?= $this->basePath('assets/vendors/datatables/css/dataTables.bootstrap5.min.css') ?>">
    <link rel="stylesheet" href="<?= $this->basePath('assets/vendors/datatables/css/responsive.bootstrap5.min.css') ?>">
    <!-- Custom styles -->
    <link href="<?= $this->basePath('assets/css/styles.css') ?>" rel="stylesheet">

    <style>
        :root {
            --primary-color: #0d47a1;
            --primary-dark: #06048C;
            --bg-light: #F8FAFC;
        }

        /* Navbar limpio */
        .navbar-vehiculos {
            background: #ffffff;
            box-shadow: 0 1px 8px rgba(0, 0, 0, 0.08);
        }

        .navbar-vehiculos .nav-link {
            color: #475569;
            font-weight: 500;
            font-size: 0.92rem;
            padding: 0.5rem 0.85rem;
            border-radius: 8px;
            transition: all 0.2s;
        }

        .navbar-vehiculos .nav-link:hover {
            color: var(--primary-color);
            background: rgba(13, 71, 161, 0.06);
        }

        .navbar-vehiculos .nav-link.active-link {
            color: var(--primary-color);
            font-weight: 700;
            background: rgba(13, 71, 161, 0.08);
        }

        /* Línea de colores APROTEC */
        .linea-colores {
            height: 5px;
            background: linear-gradient(90deg, #00bb95 25%, #ffca48 25% 50%, #ff6948 50% 75%, #00a7e6 75%);
        }

        /* Hero */
        .hero {
            height: 200px;
            position: relative;
            background-size: cover;
            background-position: center;
        }

        .hero .overlay {
            position: absolute;
            inset: 0;
            background: linear-gradient(120deg, rgba(6, 4, 140, 0.75) 0%, rgba(13, 71, 161, 0.65) 100%);
            display: flex;
            align-items: center;
        }

        /* Footer */
        .footer-vehiculos {
            background: #1e293b;
            color: #94a3b8;
            padding: 1.75rem 0;
            font-size: 0.88rem;
        }

        .footer-vehiculos a {
            color: #94a3b8;
            text-decoration: none;
        }

        .footer-vehiculos a:hover {
            color: #fff;
        }

        .footer-vehiculos .footer-title {
            color: #f1f5f9;
            font-weight: 600;
        }

        /* Dropdown usuario */
        .user-dropdown-btn {
            background: rgba(13, 71, 161, 0.08);
            border: 1.5px solid rgba(13, 71, 161, 0.2);
            color: var(--primary-color);
            border-radius: 10px;
            font-weight: 600;
            font-size: 0.88rem;
        }

        .user-dropdown-btn:hover {
            background: rgba(13, 71, 161, 0.15);
            color: var(--primary-color);
        }

        .badge-rol {
            font-size: 0.72rem;
            padding: 0.25rem 0.6rem;
            border-radius: 6px;
            font-weight: 700;
            letter-spacing: 0.04em;
        }

        .badge-admin {
            background: #fef3c7;
            color: #92400e;
        }

        .badge-insp {
            background: #dbeafe;
            color: #1e40af;
        }
    </style>

    <?= $this->headLink() ?>
    <?= $this->headStyle() ?>
</head>

<body class="d-flex flex-column min-vh-100" style="background:var(--bg-light);">

    <?php $session = new \Laminas\Session\Container('vehiculos_qr_auth'); ?>

    <!-- ============================================================
         NAVBAR PRINCIPAL
         ============================================================ -->
    <nav class="navbar-vehiculos">
        <div class="container py-2 d-flex align-items-center justify-content-between gap-3">

            <!-- Logo -->
            <a href="<?= $this->url('vehiculos-admin') ?>" class="d-flex align-items-center text-decoration-none">
                <img src="<?= $this->basePath('img/logo-aprotec-banner.png') ?>"
                    alt="Logo APROTEC"
                    style="height:48px;">
                <div class="ms-3 d-none d-sm-block lh-sm">
                    <div class="fw-bold text-primary" style="font-size:0.95rem;">Vehículos QR</div>
                    <div class="text-muted" style="font-size:0.75rem;">APROTEC</div>
                </div>
            </a>

            <!-- Nav Desktop -->
            <nav class="d-none d-md-flex align-items-center gap-1">
                <?php if (isset($session->user_id)): ?>
                    <a class="nav-link active-link" href="<?= $this->url('vehiculos-admin') ?>">
                        <i class="bi bi-qr-code me-1"></i> Gestión QR
                    </a>
                    <a class="nav-link" href="<?= $this->url('vehiculos-admin-generar-lote') ?>">
                        <i class="bi bi-plus-circle me-1"></i> Generar QR
                    </a>
                    <a class="nav-link" href="<?= $this->url('vehiculos-admin-logs') ?>">
                        <i class="bi bi-list-ul me-1"></i> Logs
                    </a>
                    <?php if (($session->rol ?? '') === 'ADMIN'): ?>
                        <a class="nav-link" href="<?= $this->url('vehiculos-admin-usuarios') ?>">
                            <i class="bi bi-people me-1"></i> Usuarios
                        </a>
                    <?php endif; ?>

                    <!-- Dropdown usuario -->
                    <div class="dropdown ms-2">
                        <button class="btn user-dropdown-btn dropdown-toggle d-flex align-items-center gap-2"
                            type="button"
                            data-bs-toggle="dropdown"
                            aria-expanded="false">
                            <i class="bi bi-person-circle fs-5"></i>
                            <span class="badge-rol <?= ($session->rol ?? '') === 'ADMIN' ? 'badge-admin' : 'badge-insp' ?>">
                                <?= $this->escapeHtml($session->rol ?? 'USUARIO') ?>
                            </span>
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end shadow-lg border-0" style="border-radius:12px; min-width:220px;">
                            <li class="px-3 py-2">
                                <div class="text-muted" style="font-size:0.75rem;">Sesión iniciada como</div>
                                <div class="fw-bold text-dark text-truncate"><?= $this->escapeHtml($session->correo ?? 'Usuario') ?></div>
                            </li>
                            <li>
                                <hr class="dropdown-divider my-1">
                            </li>
                            <li>
                                <a class="dropdown-item" href="<?= $this->url('vehiculos-editar') ?>">
                                    <i class="bi bi-pencil-square me-2 text-primary"></i> Editar mis datos
                                </a>
                            </li>
                            <li>
                                <hr class="dropdown-divider my-1">
                            </li>
                            <li>
                                <a class="dropdown-item text-danger" href="<?= $this->url('vehiculos-logout') ?>">
                                    <i class="bi bi-box-arrow-right me-2"></i> Cerrar Sesión
                                </a>
                            </li>
                        </ul>
                    </div>
                <?php else: ?>
                    <a class="btn btn-primary btn-sm ms-2" href="<?= $this->url('vehiculos-login') ?>">
                        <i class="bi bi-box-arrow-in-right me-1"></i> Ingresar
                    </a>
                <?php endif; ?>
            </nav>

            <!-- Mobile toggle -->
            <button class="btn btn-outline-secondary btn-sm d-md-none"
                type="button"
                data-bs-toggle="collapse"
                data-bs-target="#mobileMenu"
                aria-expanded="false">
                <i class="bi bi-list fs-5"></i>
            </button>
        </div>

        <!-- Línea de colores APROTEC -->
        <div class="linea-colores"></div>

        <!-- Mobile Menu -->
        <div class="collapse" id="mobileMenu">
            <div class="container py-3 border-top">
                <ul class="nav flex-column gap-1">
                    <?php if (isset($session->user_id)): ?>
                        <li class="nav-item">
                            <a class="nav-link active-link" href="<?= $this->url('vehiculos-admin') ?>">
                                <i class="bi bi-qr-code me-2"></i> Gestión QR
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="<?= $this->url('vehiculos-admin-generar-lote') ?>">
                                <i class="bi bi-plus-circle me-2"></i> Generar QR
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="<?= $this->url('vehiculos-admin-logs') ?>">
                                <i class="bi bi-list-ul me-2"></i> Logs
                            </a>
                        </li>
                        <?php if (($session->rol ?? '') === 'ADMIN'): ?>
                            <li class="nav-item">
                                <a class="nav-link" href="<?= $this->url('vehiculos-admin-usuarios') ?>">
                                    <i class="bi bi-people me-2"></i> Usuarios
                                </a>
                            </li>
                        <?php endif; ?>
                        <li class="nav-item">
                            <a class="nav-link" href="<?= $this->url('vehiculos-editar') ?>">
                                <i class="bi bi-pencil-square me-2"></i> Editar datos
                            </a>
                        </li>
                        <li class="nav-item mt-2">
                            <a class="nav-link text-danger" href="<?= $this->url('vehiculos-logout') ?>">
                                <i class="bi bi-box-arrow-right me-2"></i> Cerrar Sesión
                            </a>
                        </li>
                    <?php else: ?>
                        <li class="nav-item">
                            <a class="nav-link fw-bold text-primary" href="<?= $this->url('vehiculos-login') ?>">
                                <i class="bi bi-box-arrow-in-right me-2"></i> Ingresar al Sistema
                            </a>
                        </li>
                    <?php endif; ?>
                </ul>
                <div class="mt-3 pt-2 border-top">
                    <small class="text-muted">
                        <?= $this->escapeHtml($session->correo ?? '') ?>
                        <?php if (isset($session->rol)): ?>
                            · <span class="badge-rol <?= ($session->rol ?? '') === 'ADMIN' ? 'badge-admin' : 'badge-insp' ?> px-2">
                                <?= $this->escapeHtml($session->rol) ?>
                            </span>
                        <?php endif; ?>
                    </small>
                </div>
            </div>
        </div>
    </nav>

    <!-- ============================================================
         HERO (solo en páginas que no lo desactivan)
         ============================================================ -->
    <?php if (!isset($this->layout()->{'visible-hero'}) || $this->layout()->{'visible-hero'}): ?>
        <div class="hero mb-4"
            style="background-image:url('<?= $this->basePath('assets/img/hero-vehiculos.jpg') ?>');">
            <div class="overlay">
                <div class="container text-white">
                    <?php
                    $heroTitle = '';
                    if (isset($this->layout()->heroTitle) && trim((string) $this->layout()->heroTitle) !== '') {
                        $heroTitle = (string) $this->layout()->heroTitle;
                    } else {
                        $heroTitle = trim((string) $this->placeholder('heroTitle'));
                    }
                    if ($heroTitle === '') {
                        $heroTitle = 'Sistema de Identificación Vehicular';
                    }
                    ?>
                    <h1 class="fw-bold mb-2" style="font-size:clamp(1.4rem,3vw,2rem);">
                        <?= $this->escapeHtml($heroTitle) ?>
                    </h1>
                    <?php
                    $heroSubtitle = '';
                    if (isset($this->layout()->heroSubtitle) && trim((string) $this->layout()->heroSubtitle) !== '') {
                        $heroSubtitle = (string) $this->layout()->heroSubtitle;
                    } else {
                        $heroSubtitle = trim((string) $this->placeholder('heroSubtitle'));
                    }
                    if ($heroSubtitle !== ''): ?>
                        <p class="mb-0 text-white-75" style="font-size:0.95rem;">
                            <?= $this->escapeHtml($heroSubtitle) ?>
                        </p>
                    <?php endif; ?>
                    <div class="mt-3">
                        <span class="badge"
                            style="background:rgba(255,255,255,0.2); font-size:0.78rem; border-radius:8px; padding:0.3rem 0.8rem; backdrop-filter:blur(4px);">
                            <i class="bi bi-building me-1"></i> APROTEC
                        </span>
                    </div>
                </div>
            </div>
        </div>
    <?php else: ?>
        <div style="height:1rem;"></div>
    <?php endif; ?>

    <!-- ============================================================
         CONTENIDO PRINCIPAL
         ============================================================ -->
    <main class="flex-grow-1">
        <div class="container pb-5">
            <?= $this->content ?>
        </div>
    </main>

    <!-- ============================================================
         FOOTER
         ============================================================ -->
    <footer class="footer-vehiculos mt-auto">
        <div class="container">
            <div class="row align-items-center gy-3">
                <div class="col-md-4 text-center text-md-start">
                    <img src="<?= $this->basePath('img/logo-aprotec-banner.png') ?>"
                        alt="Logo APROTEC"
                        style="height:36px; filter:brightness(0) invert(0.6);">
                </div>
                <div class="col-md-4 text-center">
                    <div class="footer-title mb-1">APROTEC</div>
                    <div>Sistema de Identificación Vehicular QR</div>
                </div>
                <div class="col-md-4 text-center text-md-end">
                    <div>
                        <i class="bi bi-globe me-1"></i>
                        <a href="https://www.aprotec.cl" target="_blank">www.aprotec.cl</a>
                    </div>
                    <div class="mt-1">
                        <i class="bi bi-shield-check me-1"></i> Control y Gestión Digital · <?= date('Y') ?>
                    </div>
                </div>
            </div>
        </div>
    </footer>

    <!-- Scripts -->
    <script src="<?= $this->basePath('assets/vendors/jquery/jquery-3.7.1.min.js') ?>"></script>
    <script src="<?= $this->basePath('assets/vendors/bootstrap/js/bootstrap.bundle.min.js') ?>"></script>
    <!-- DataTables JS -->
    <script src="<?= $this->basePath('assets/vendors/datatables/js/jquery.dataTables.min.js') ?>"></script>
    <script src="<?= $this->basePath('assets/vendors/datatables/js/dataTables.bootstrap5.min.js') ?>"></script>
    <script src="<?= $this->basePath('assets/vendors/datatables/js/dataTables.responsive.min.js') ?>"></script>
    <!-- SweetAlert2 -->
    <script src="<?= $this->basePath('assets/vendors/sweetalert2/sweetalert2.all.min.js') ?>"></script>
    <!-- App JS -->
    <script src="<?= $this->basePath('assets/js/app.js') ?>"></script>
    <?= $this->inlineScript() ?>
</body>

</html>

