RewriteEngine On
RewriteBase /aprotec/paginaweb/public/

# No redirigir si el archivo o directorio ya existe
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]